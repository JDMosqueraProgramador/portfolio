import React from 'react'
import { IProject } from '../interfaces/project.interface';

const Project: React.FC<IProject> = ({ img, description, name, technologies }) => {
    return (
        <div>Project</div>
    )
}

export default Project;